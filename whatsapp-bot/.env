# Configuración de Base de Datos MySQL (Railway)
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_USER=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}

# Configuración de la API Flask (se actualizará en Railway)
API_URL=http://localhost:5000/api

# Configuración del servidor de URLs (mantener existente)
URL_SERVER=https://api-production-f9fb.up.railway.app
URL_SERVER_API_KEY=clave-secreta-cambiar

# Puerto para Railway
PORT=8080

# Configuración para producción
NODE_ENV=production

# Configuración de Puppeteer para Railway
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
NODE_ENV=production
